<!DOCTYPE html>
<html lang="es">

<%- include("partials/head") -%>

    <body class="products">
        <div class=container>
            <%- include("partials/header") -%>
                <hr>
                <main>
                    <div class="proDet">
                                <h3>DETALLE DEL PRODUCTO</h3>
                            

                            <div class="productsDetails">

                                <img width="200" height="200" class="imagen" src="../img/<%= product.avatar %>">
                                <div class="descrip">
                                    <li>Marca: <%= product.marca %></li>
                                    <li>Modelo: <%= product.modelo %></li>
                                    <li>Rodado: <%= product.rodado %></li>
                                    <li>Ancho:  <%= product.ancho %></li>
                                    <li>Perfil: <%= product.perfil %></li>
                                    <li>Precio: <%= product.precio %></li>
                                    
                                </div>

                                <% if (isLogged) { %>
                                    <div class="botones">
                                        <a href="/editProduc/<%= product.ID %>">Editar</a>
                                    </div>
                                
                                    <form method="POST" action="/delete/<%= product.ID %>?_method=DELETE">
                                        <button type="submit">Eliminar</button>
                                    </form>
                                    <% } %>
                            </div>
                    </div>
                </main>

                
        </div>
        <%- include("partials/footer") -%>

    </body>

</html>